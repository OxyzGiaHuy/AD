\appendix
\section{Supplementary tables}\label{app:tables}
This appendix collects supporting tables whose content is summarized in the main text: the per-corruption VisA ECE breakdown (Table~\ref{tab:visa-ece-corruption}; Figure~\ref{fig:ece-corruption} visualizes the same comparison), the aggregation-sensitivity ablation (Table~\ref{tab:agg-ablation}), and the novelty-positioning summary (Table~\ref{tab:prior-positioning}).

\input{tables/tab_visa_ece_by_corruption}
\input{tables/tab_agg_ablation}
\input{tables/tab_prior_work_positioning}
